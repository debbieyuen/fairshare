# FairShare doc images

Put walk-through screenshots here (PNG or WebP). Reference them from Markdown under `docs/` with paths relative to the `.md` file, e.g. from `docs/fairshare-overview.md` use `images/your-file.png`.
